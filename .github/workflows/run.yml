name: run
on:
  workflow_dispatch: 
jobs:
  run:
    runs-on: macos-latest
    env:
      GITHUB_TOKEN: ${{ secrets.token }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
          ruby-version: 3.0

      - name: script/bootstrap
        run: script/bootstrap
      
      - name: clone repos
        run: script/clone ZscalerCWP

      - name: count
        working-directory: count-org-loc/count-org-loc
        run: script/stats.sh
        shell: bash   
